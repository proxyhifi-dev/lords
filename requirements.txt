# Lords Bot v5.1 — Production Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Settings management
pydantic-settings>=2.2.0

# Numerical / data
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# Database & Caching (PRODUCTION)
redis>=5.0.0
psycopg2-binary>=2.9.0
sqlite3  # Built-in, no install needed

# Monitoring & Observability (PRODUCTION)
prometheus-client>=0.20.0
structlog>=24.1.0

# SAMCO broker SDK (install separately from SAMCO)
# snapi-py-client  — see README for installation

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.2.0
httpx>=0.27.0
